# Webapp Environment Configuration (Cloud Environments)
# Processed with envsubst during CI/CD build
# Environment-specific values come from GitHub environment secrets/variables

VITE_API_CENTER_BASE_URL=${CENTER_URL}/api
VITE_CLERK_PUBLISHABLE_KEY=${CLERK_PUBLISHABLE_KEY}
