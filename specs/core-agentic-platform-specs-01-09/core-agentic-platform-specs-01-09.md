# Core Agentic Platform — Specs 1–9 (MVP)

> This document collects Specs 1–9 into a single file for easy sharing and iteration.

---

## Spec 1 — Skills Catalog (Core Agentic Platform)

(See `spec-01-skills-catalog.md`)

---

## Spec 2 — Skill Runner (NestJS) + Execution Model

(See `spec-02-skill-runner.md`)

---

## Spec 3 — Provider Adapters (Thin Wrappers)

(See `spec-03-provider-adapters.md`)

---

## Spec 4 — Run Engine (Workflow Orchestrator + Partial Rebuild)

(See `spec-04-run-engine.md`)

---

## Spec 5 — Prompt & Config Registry (Templates + Rubrics)

(See `spec-05-prompt-config-registry.md`)

---

## Spec 6 — Agent Layer Decision Rules (Claude Agent SDK vs LangGraph)

(See `spec-06-agent-layer-rules.md`)

---

## Spec 7 — Default Workflow Pack v1 (Campaign Build + Update Workflows)

(See `spec-07-default-workflow-pack.md`)

---

## Spec 8 — Campaign Context Model (runtime contract passed between steps)

(See `spec-08-campaign-context-model.md`)

---

## Spec 9 — Reference Implementations (Starter Code Map + 3 Concrete Skills + 1 Workflow Run)

(See `spec-09-reference-implementations.md`)
