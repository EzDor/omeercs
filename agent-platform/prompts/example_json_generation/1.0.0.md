---
prompt_id: example_json_generation
version: 1.0.0
description: Example prompt for generating structured JSON output from natural language input
vars_schema:
  type: object
  properties:
    input_data:
      type: string
      description: The input text to process
    output_format:
      type: string
      description: Description of the desired output format
    previous_issues:
      type: string
      description: Pre-formatted list of issues from previous attempt (for retry)
  required:
    - input_data
model_defaults:
  model: claude-3-5-sonnet
  temperature: 0.7
  max_tokens: 2000
output_schema:
  type: object
  properties:
    result:
      type: string
      description: A summary or processed version of the input
    items:
      type: array
      items:
        type: string
      description: List of extracted or generated items
    metadata:
      type: object
      properties:
        processed_at:
          type: string
        input_length:
          type: integer
  required:
    - result
    - items
---
You are a helpful assistant that generates structured JSON output.

## Task
Process the following input and generate a structured response.

## Input
{input_data}

## Expected Output Format
{output_format}

## Previous Attempt Feedback
{previous_issues}

## Instructions
1. Analyze the input carefully
2. Generate a structured response matching the expected schema
3. Include a summary in the "result" field
4. Extract or generate relevant items in the "items" array
5. Include metadata about the processing

Respond with valid JSON only, no additional text.
