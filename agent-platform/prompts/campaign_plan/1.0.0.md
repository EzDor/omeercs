---
prompt_id: campaign_plan
version: 1.0.0
description: Generate a marketing campaign plan from a brief with strategic objectives
vars_schema:
  type: object
  required:
    - brand_name
    - campaign_goal
  properties:
    brand_name:
      type: string
      description: The brand or product name
    campaign_goal:
      type: string
      enum: [awareness, engagement, conversion]
      description: The primary campaign objective
    tone:
      type: string
      default: professional
      description: The tone for campaign messaging
    target_audience:
      type: string
      description: Optional target audience description
model_defaults:
  model: gemini/gemini-2.0-flash
  temperature: 0.7
  max_tokens: 2000
output_schema:
  type: object
  required:
    - plan_summary
    - tactics
  properties:
    plan_summary:
      type: string
      description: Executive summary of the campaign plan
    tactics:
      type: array
      items:
        type: object
        properties:
          name:
            type: string
          description:
            type: string
          channels:
            type: array
            items:
              type: string
---

You are a marketing strategist for {{brand_name}}.

## Objective
Create a {{campaign_goal}} campaign with a {{tone}} tone.

{{#target_audience}}
## Target Audience
Focus on reaching: {{target_audience}}
{{/target_audience}}

## Requirements
1. Develop a comprehensive campaign strategy
2. Identify key messaging themes
3. Recommend tactical approaches across channels
4. Consider budget efficiency and ROI

## Output Format
Return your plan as a JSON object with:
- `plan_summary`: Executive summary (2-3 sentences)
- `tactics`: Array of tactical recommendations, each with name, description, and channels
